# Screen Landmark Lens — independent verification 4

## Result: FAIL

- Candidate commit: `9085ecdbaa62d0dd90008017b0c2495387f19787`
- Live URL: <https://screen-landmark-lens.sociobot.in>
- Verification date: 2026-09-02 UTC

The static website generated by this candidate matches production byte-for-byte, but the Download actions still distribute desktop packages built from `10e7782b03b146831a952f7b2dca8ae238674616`, not this candidate. A desktop-app release is not accepted until its installable artifacts and manifest identify the candidate commit.

## First-read check — PASS

A cold live visit says **“Find the control your screen reader can’t.”** It explains that it is for people using a remote desktop or legacy-app window whose visible labels need local reading and a directional cue. The obvious first action is **Try it with sample data**. It is one click and opens the bundled legacy-form demo. The first screen also plainly states device-local capture, offline use after installation, and free core tools.

## Claim contract — PASS after documented desktop prerequisites

`.factory/claims.json` exists and has 26 entries. From a clean checkout at the candidate, I ran `npm ci` and then every exact `test` command in the manifest. The bare container initially lacked the documented Tauri Linux libraries, so the three native claims could not locate `glib-2.0`; after installing the exact Linux dependency set named by the release workflow, all 26 passed. The slow local-model claim completed in 427.97 seconds without a service.

| Claim | Result |
| --- | --- |
| `demo-sample` | PASS |
| `demo-privacy` | PASS |
| `website-demo-storage` | PASS |
| `website-privacy` | PASS |
| `offline-demo` | PASS |
| `site-updates` | PASS |
| `desktop-sample` | PASS |
| `demo-mode-isolation` | PASS |
| `free-voice-speed` | PASS |
| `desktop-shortcuts` | PASS |
| `screen-reader-announcements` | PASS |
| `blank-find` | PASS |
| `ocr-uncertainty` | PASS |
| `local-processing` | PASS |
| `selected-window` | PASS |
| `capture-discarded` | PASS |
| `guidance-only` | PASS |
| `no-account-required` | PASS |
| `release-metadata-cache` | PASS |
| `release-metadata-fallback` | PASS |
| `unknown-route-404` | PASS |
| `release-assets` | PASS for the published `v0.1.5` tag, not for this candidate |
| `package-signatures` | PASS |
| `checksum-installers` | PASS |
| `dependency-licenses` | PASS |
| `image-provenance` | PASS |

## Local quality checks

| Check | Result |
| --- | --- |
| `npm test` | PASS — TypeScript, copy audit, 14 shared assertions, and 7 Rust tests |
| `npm run test:web` | PASS — 45 passed; 1 intentional mobile-only geometry skip |
| `npm run test:app-web` | PASS — 28/28 |
| `npm run build` | PASS — deployable `dist/site` and `dist/app` |
| `npm run tauri -- build --bundles deb` | PASS — candidate Linux DEB built locally: `screen-landmark-lens` 0.1.5, amd64, SHA-256 `8dd7e58f950553df82fc0be67b3d57500430400bfcdec4c1b5f566b4337d69c9` |
| `cargo fmt --manifest-path src-tauri/Cargo.toml -- --check` | PASS |
| `CARGO_BUILD_JOBS=1 cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets -- -D warnings` | PASS |
| `npm audit --omit=dev` | PASS — 0 vulnerabilities |
| AppImage local bundling | Environment-limited — `linuxdeploy` cannot run in this container; see known gap |

The generated site bundle is 1.91 KB gzip JavaScript and 3.71 KB gzip CSS. The app bundle is 3.39 KB gzip JavaScript and 2.78 KB gzip CSS, all inside the stated budgets.

## Live, accessibility, privacy, and performance checks

- Desktop cold load, 390×844 demo, and reduced-motion cold load all returned HTTP 200 with zero page or console errors.
- A live `Save` sample result reads: `Found OCR text “Save”, bottom right.` The full browser suites cover empty and unmatched searches, shortcuts, keyboard operation, demo reset/isolation, and spoken/live-region announcements.
- Cold request logs contained only the product origin. No third-party scripts, cookies, trackers, analytics, fonts, cloud vision, or capture uploads appeared. The landing release lookup is bootstrapped locally; its CSP only permits the separately documented `https://api.github.com` fallback.
- First Tab on the landing page focused the visible skip link; its focus ring is a 3px solid outline. Axe on live desktop, mobile, and reduced-motion pages reported no serious or critical findings.
- `/opt/fleet/lib/verify-url.sh` passed: `lang=en`, one H1, main landmark, image alt text, labelled buttons, and zero console errors; measured load was 633 ms.
- Live headers include HSTS, `nosniff`, `no-referrer`, restrictive permissions policy, `X-Frame-Options: DENY`, and a self-only CSP plus GitHub API fallback. HTML revalidates in 30 seconds, hashed assets are immutable for one year, and `sw.js` is `no-cache`.
- Service-worker offline demo reload and network-first update behavior passed their dedicated claim tests.
- Lighthouse mobile (live): performance **100**, accessibility **100**, FCP **1.0 s**, LCP **1.0 s**, TBT **10 ms**, CLS **0.008**.
- `index.html` and `assets/main-Cuz88eN4.js` from the live site SHA-256 match the candidate-generated `dist/site` files exactly. This is expected because this candidate changes tests/evidence, not website runtime source.

## Deployment and release evidence

- Live landing, demo, privacy, and terms returned 200. An unknown route returned the designed 404.
- Release `v0.1.5` contains all macOS, Windows, and Linux assets, `SHA256SUMS`, `latest.json`, and unsigned-package reports.
- A fresh 14,831,824-byte `Screen-Landmark-Lens_0.1.5_linux.deb` has SHA-256 `3d1eee1f222cf8f0edfdbeb74ef20d8d12f3394a4fe2008cca3f8ad17ea2a347`, matching `SHA256SUMS`.
- However, the GitHub release target and published `latest.json` both name `10e7782b03b146831a952f7b2dca8ae238674616`; `git rev-parse 9085ecd` is different. Candidate `9085ecd` has no release tag. The candidate’s `release-assets` claim passes only because it validates `v0.1.5` against that older tag, not the candidate under review.
- There is no product server-side API or unlock endpoint; rate-limit verification does not apply.

## Defects by severity

### Critical — published desktop artifacts do not match candidate `9085ecd`

The live landing downloads `v0.1.5` packages and `latest.json` declares source `10e7782…`, while the candidate is `9085ecd…`. This violates the required deployment/candidate identity check for a desktop app. Publish a new version/tag from `9085ecd` (or an equivalent commit containing it), wait for all release matrix artifacts and `latest.json`, then repoint/redeploy the landing metadata and reverify checksums.

### Medium — local AppImage packaging is not reproducible in this verifier container

Even after the release workflow's listed dependencies and `xdg-utils` were installed, `npm run tauri -- build --bundles appimage,deb` stopped at `failed to run linuxdeploy`. The candidate Linux DEB does build successfully. GitHub’s existing Ubuntu matrix run has already produced the older AppImage, so this does not explain the critical deployment mismatch; nevertheless, document the required AppImage/FUSE runtime or use a container-compatible packaging path for local verification.

## Known gaps

This environment has no interactive desktop session, so physical capture of a separately rendered window could not be repeated here. The bundled native model, selected-window boundary, capture-discard boundary, and full keyboard/speech demo flows all passed their native and browser checks. The brief's blind/low-vision pilot success measure remains a real-world research follow-up, not a release claim.
